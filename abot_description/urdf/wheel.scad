% scale(1000) import("wheel.stl");

// Append pure shapes (cube, cylinder and sphere), e.g:
// cube([10, 10, 10], center=true);

rotate([0,90,0])
    cylinder(r=25, h=18, center=true);
// sphere(10);
